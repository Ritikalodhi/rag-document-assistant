#── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# ── LangChain ecosystem ───────────────────────────────────────────────────────
# Versions here all pull langchain-core>=1.x together — no pin conflicts.
langchain>=0.3.27
langchain-community>=0.3.27
langchain-openai>=0.2.14

# langchain-google-genai 4.x dropped the old google-generativeai SDK entirely.
# It now uses google-genai (the new unified SDK) — do NOT add google-generativeai
# alongside it or you will get a google-ai-generativelanguage version conflict.
langchain-google-genai>=4.0.0

# ── Vector database ───────────────────────────────────────────────────────────
# langchain-chroma >=1.0.0 requires chromadb >=1.0.0
chromadb>=1.0.0
langchain-chroma>=1.0.0

# ── Document parsing ──────────────────────────────────────────────────────────
pypdf>=4.2.0
# used by Docx2txtLoader in langchain-community
docx2txt>=0.8   
# kept for any direct python-docx usage       
python-docx>=1.1.2     

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic>=2.8.2
email-validator>=2.2.0
requests>=2.32.3
loguru>=0.7.2
pandas>=2.2.2
markdown>=3.6

rank-bm25>=0.2.2
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
redis>=5.0.0

# ── Cross-encoder re-ranker ──────────────────────────────────────────────────
# Install separately if you want re-ranking (pip install sentence-transformers).
sentence-transformers>=3.4.0

# ── File locking for concurrent-safe JSON stores ─────────────────────────────
filelock>=3.13.0
